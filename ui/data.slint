// Generated Data Model

export struct AppData {
    current-temp: float,
    target-temp: float,
    humidity: float,
    hvac-status: string,
    schedule-status: string,
    temp-unit: string,
    eco-mode: bool,
    work-hours-start: string,
    work-hours-end: string,
    weekend-mode: bool,
    brightness: float,
    auto-dim: bool,
    app-version: string,
}
